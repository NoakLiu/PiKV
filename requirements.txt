# PiKV core dependencies (2026)
# Install: pip install -r requirements.txt && pip install -e .
# GPU PyTorch: prefer conda/pip wheels from pytorch.org (see install_pikv.sh / environment.yml)

# --- Deep learning ---
torch>=2.2.0,<2.7
torchvision>=0.17.0
torchaudio>=2.2.0
transformers>=4.40.0
datasets>=2.19.0
accelerate>=0.30.0
huggingface_hub>=0.23.0
safetensors>=0.4.0
sentencepiece>=0.2.0
tokenizers>=0.19.0

# --- Numeric / scientific ---
# Pin below 2.x until full stack is validated with NumPy 2
numpy>=1.26.0,<2.0.0
scipy>=1.11.0
pandas>=2.1.0
scikit-learn>=1.3.0

# --- Training / logging ---
tqdm>=4.66.0
wandb>=0.17.0
tensorboard>=2.14.0
psutil>=5.9.0
matplotlib>=3.8.0

# --- Eval helpers (NTP / protocol / LongBench-lite) ---
evaluate>=0.4.0

# Optional heavy stacks (install separately if needed):
#   pip install deepspeed          # distributed training
#   pip install "vllm>=0.5.0"      # vLLM integration path
#   pip install peft               # LoRA fine-tuning extras
